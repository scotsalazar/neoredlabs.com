/**
 * PostCSS configuration.  Tailwind CSS and Autoprefixer are used
 * together to generate the final CSS.  Vite picks up this file
 * automatically when building the project.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};